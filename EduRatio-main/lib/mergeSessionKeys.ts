/** Keys for Merge portal auth context in sessionStorage (guidelines §7.2). */
export const MERGE_SESSION_STORAGE_KEYS = {
  token: "merge_token",
  studentId: "merge_student_id",
  sessionId: "merge_session_id",
} as const;
